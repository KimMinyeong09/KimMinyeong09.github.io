# Motion videos

Put motion videos here using this filename pattern:

```text
G031T002A008R005_0.mp4
G043T000A003R025_0.mp4
```

The evaluator builds video paths from `m1.name` and `m2.name` in the JSON:

```javascript
assets/videos/{motion_name}_0.mp4
```
