# Data files

Place the three data quality JSON files here:

```text
data-quality-1.json
data-quality-2.json
data-quality-3.json
```

Each file should be a JSON array with items shaped like:

```json
{
  "id": 6026,
  "m1": { "name": "G031T002A008R005", "org_des": "..." },
  "m2": { "name": "G043T000A003R025", "org_des": "..." },
  "instruction": {
    "final": "Change push into a walking approach, arm grab, and forward pull."
  }
}
```
